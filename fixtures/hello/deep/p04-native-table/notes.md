# Native table page

The table compares what each engine owns. pptwise handles bullets and cards, while ppt-master handles charts, tables and formulas.

Only the deep engine output stays editable as native objects.
